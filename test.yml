---
- name: Test playbook on localhost
  hosts: localhost
  connection: local
  gather_facts: no

  tasks:
    - name: Print message
      debug:
        msg: "Hello from AWX! Automation is working."

    - name: Show current date
      command: date
